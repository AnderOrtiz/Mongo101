use("sample_training")

db.trips.find({ $and:[
    {$expr:{$eq:["$start station id", "$end station id"]}},
    {$nor:[{usertype: "Subscriber"}]},
    // {$nq:[{"end station location.type": "Point"}]}
]})//.count()

